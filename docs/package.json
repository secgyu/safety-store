{
  "name": "docs-capture",
  "version": "1.0.0",
  "description": "Screenshot and video capture scripts for documentation",
  "scripts": {
    "capture": "node capture-screenshots.js",
    "capture:landing": "node capture-landing.js",
    "capture:results": "node capture-results.js",
    "capture:all": "npm run capture && npm run capture:landing && npm run capture:results",
    "convert": "node convert-to-gif.js",
    "all": "npm run capture:all && npm run convert"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "fluent-ffmpeg": "^2.1.3",
    "playwright": "^1.40.0"
  }
}
